# Markhand Web

Browser-only React/Vite shell for the future Phase 2 SPA. It must communicate through
the generated OpenAPI client and browser HTTP/SSE only; never import `@tauri-apps/*`
or desktop IPC.

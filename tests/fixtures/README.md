# Shared fixtures

Only cross-crate deterministic fixtures live here. Component-local fixtures remain
beside their tests but follow the same manifest/security rules.

`sample/document.txt` is synthetic Vietnamese content used to verify the validator.
Update `manifest.json` checksum whenever a reviewed fixture changes.
